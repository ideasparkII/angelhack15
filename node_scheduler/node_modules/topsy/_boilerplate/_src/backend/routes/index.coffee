
###
API
###

router = require('express').Router()
env   = require("#{__dirname}/../env.json")

## GET home page
router.get '/', (req, res) ->
  res.render('index', { title: "#{env.NAME}" })

module.exports = router
