define [
  'backbone'
  'eventBus'
  'routers/router'
], (Backbone, EventBus, Router) ->

  class {{ App }}

    start: ->
      _{{ App }}.router = new Router

      # NOTE: Backbone.Events may be sufficient
      _{{ App }}.eventBus = new EventBus

      # NOTE: PushState seems to break using github pages
      Backbone.history.start()

